name: Django CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}        # Usando a variável secret do GitHub
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}    # Usando a variável secret do GitHub
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}  # Usando a variável secret do GitHub
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run migrations
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}       # Usando a variável secret do GitHub
          DJANGO_SETTINGS_MODULE: xclone.settings        # Ajustado para o novo nome do diretório
        run: |
          python manage.py migrate

      - name: Run tests
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}       # Usando a variável secret do GitHub
          DJANGO_SETTINGS_MODULE: xclone.settings        # Ajustado para o novo nome do diretório
        run: |
          python manage.py test

      - name: Run flake8
        run: |
          pip install flake8
          flake8
